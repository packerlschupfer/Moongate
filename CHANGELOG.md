# Changelog

All notable changes to Moongate, newest first. Versions follow `vMAJOR.MINOR.PATCH+BUILD` where `BUILD` is a monotonic integer used by the in-app update banner.

| Version | Changes |
|---|---|
| **v0.9.42** | **Better multi-toolhead support: reliable detection, per-tool preheat, and all temps in notifications.** **(1) More reliable detection.** Tool-changer printers (klipper-toolchanger / StealthChanger) are now recognised directly from the printer's `[toolchanger]` + `[tool …]` setup, reading each tool's own extruder and the active tool, so **every hotend shows on the dashboard** even with unusual heater naming. Plain single-hotend and IDEX printers fall back to the original extruder detection, unchanged. **(2) Per-tool preheat.** Long-pressing a multi-hotend printer's temperatures now opens a Preheat sheet with **one compact row per tool** - **T0, T1, T2 ...**, each with its own target field - plus **Bed**, so you can set **every hotend at once** instead of only the first (one `SET_HEATER_TEMPERATURE` per tool over the existing secure path, no Pi update); single-hotend printers keep the classic Hotend + Bed fields. **(3) Multi-toolhead notifications.** The persistent print notification's **hotend segment** now lists **every tool's live temperature** on one line (e.g. `🔥210° 205° 25°`) while a print runs, instead of just the first tool; single-hotend printers are unchanged. **(4) Fixed:** the **"Printer unreachable"** page (Cloudflare-tunnel errors) was an unsized panel that only covered a centre strip, leaving the failed web page showing through down the side - it now **fills the whole view** in your theme. App-only update |
| **v0.9.41** | **Optional dashboard buttons.** A new **Show dashboard buttons** switch (menu → Dashboard Layout) hides the floating **add** and **reorder** buttons at the bottom of the dashboard, useful when you have a lot of printers and the buttons cover the bottom tiles. Adding a printer stays available from the **menu**, and reordering by turning the switch back on. On by default; saved and carried in backups. Localised in all 8 languages. App-only update |
| **v0.9.40** | **Dashboard temperature layout, rebuilt to size consistently.** After the v0.9.39 font update, each tile's **hotend / bed / chamber** temperatures could **shrink to tiny text** on busier or narrower tiles (the group was fighting a spacer for width, or scaling itself down to fit), so different tiles ended up with different text sizes. The temperature row is now rebuilt around one shared layout: every chip is **one consistent size set only by the display-size slider**, and when a tile is too narrow the chips **wrap onto another row** (or **stack** the icon above the number) instead of ever shrinking. **Multi-toolhead** printers use the width better too, on a wide single-column tile all the tool temperatures sit on **one line** with the bed and chamber, flowing onto extra rows only when the tile is narrow, and the **E-STOP** stays pinned at the right and is never clipped. App-only update |
| **v0.9.39** | **Multi-toolhead dashboard tiles, plus a much bigger font picker.** Printers with **more than one toolhead** (IDEX, tool changers) now show **every hotend** on the dashboard tile: each tool gets its own temperature labelled **T0, T1, T2 ...**, wrapping to a small grid, with the tool **currently in use highlighted** and the **bed and chamber** dropped to a row beneath. Every detected tool is shown whether it's heating or not, and single-toolhead printers are unchanged. It's app-side only (the extra hotends are read over the existing secure path), so no Pi update is needed. **Also:** the **Font** option (menu → Theme) now offers **30-plus typefaces** grouped by style (sans, rounded, serif, slab, monospace, techy/display, condensed, handwriting and retro/pixel), each previewed in its own font, and tight spots like **printer names** and the **temperature row** now **auto-shrink** so nothing clips whatever you choose. **Fixed:** the **persistent status notification** now follows your **dashboard order** - with **"Auto-arrange by status"** off (printers left in the order you arranged them), the notification lists them the same way instead of always floating the busiest printer to the top. App-only update |
| **v0.9.38** | **Pick the app's font, plus a tutorial polish.** A new **Font** option in the menu (under Theme) switches the whole app between a **Default**, **Rounded**, **Serif**, and **High-readability** typeface; your choice is saved and travels in backups. Apps can't read the specific font you've set in your phone's own settings, so Moongate ships a small built-in set (Nunito / Lora / Atkinson Hyperlegible) instead. Also, in the first-run tour's temperatures step, the **hotend, bed and chamber** readings are now highlighted inside a **single box** together rather than a separate ring around each. App-only update |
| **v0.9.37** | **Phone colours theme (Material You) + a tighter tutorial.** On **Android 12 and newer**, a new **Phone colours** theme option matches your phone's own **wallpaper-based palette** (the one Android calls Material You) and follows its **light/dark** setting, sitting alongside **Dark / Light / Custom**. It only appears where the OS actually provides a palette - older Android and **iPhone** have none, so it stays hidden there and those devices keep the default theme. (This is distinct from the old "System default" option removed in v0.9.11, which only followed light/dark, not the wallpaper colours.) The first-run **tutorial** is also trimmed: the **hotend, bed and chamber** temperatures are now introduced **together in one step** instead of three, it no longer walks the self-explanatory **Theme**, **About**, **Settings** and **Buy me a coffee** menu entries, and the early-exit button now reads **End** instead of **Skip**. Localised in all 8 languages. App-only update |
| **v0.9.36** | **A quick guided tour of the app.** After you add your first printer, Moongate now offers an optional **in-app tutorial** that highlights each part of the app live: the **green / orange connection bars** and what they mean, the **"tunnel still building"** marker you see just after pairing, the **hotend / bed / chamber** temperatures, the **double-tap emergency stop**, the **webcam**, and **preheat**, then it opens the **menu** and walks every option in it. Move through it with **Next** / **Back**, leave with **Skip**, and run it again any time from the **bottom of the menu**. It's offered once and remembers your choice. **Also:** the press-and-hold target that opens **preheat** is now the whole tile (name + temperatures), so it's far easier to trigger (it had become fiddly at larger display sizes), and **Delete my data** has moved into **Settings** next to **Remove all printers**. Localised in all 8 languages. App-only update |
| **v0.9.35** | **Display-size scaling for the whole dashboard, plus a first-run plugin reminder.** The menu size slider (renamed **"Font size"** to **"Display size"**) now scales the dashboard **icons** as well as the text, so the whole dashboard grows and shrinks together instead of leaving the icons at a fixed size; the **emergency-stop** button scales with it and is a touch smaller by default. At larger sizes each tile's **hotend / bed / chamber** temperatures **stack into two rows** (icons above values) so they no longer run off a narrow tile. A new **first-run notice** at the top of **"How pairing works"** explains that the **Moongate plugin** must be installed on the printer's Pi first, with a one-tap link to the setup guide (and the usual "don't show again"). Plus a repo-wide **text and punctuation** tidy-up. App-only update |
| **v0.9.34** | **Power all machines now works with Klipper-macro power, plus off-only setups.** The dashboard's **"Power all machines"** button switched only Moonraker smart-plug power devices, so a printer powered by a **Klipper macro** (set in the **Advanced Power Switch**) did nothing. It now switches each printer the way that printer is set up, and reads each one's real capability: it shows **Power on all** / **Power off all** only when machines support them, and labels each machine as on / off, off only, on only, toggle only, printing or offline. The **Advanced Power Switch** now turns on with **just a Power Off macro** (or just a Power On macro), not only a full pair or a toggle, which suits the common "I only switch them off" setup; a single toggle macro still drives the per-tile power button. **Also:** after an in-app update installs, the downloaded APK is cleared from the cache instead of lingering, so the app stops using that extra storage. App-only update |
| **v0.9.33** | **Global power on/off for your whole fleet.** A new **optional** power button in the dashboard top bar (turn it on in the menu, it is off by default since it needs Moonraker power devices). It opens a **"power all machines"** sheet that switches every reachable machine's smart-plug power **on** with a tap, or **off** behind a deliberate **slide-to-confirm**. Safety first: machines that are **printing are left on** (Moonraker's `locked_while_printing` is a second backstop), **unreachable** ones are skipped, and the sheet lists **exactly which machines will switch** before you commit, so a smart plug that also feeds the Pi is visible up front. App-only, reusing the existing Moonraker power API over the secure path, and localised in all 8 languages. **Also, an optional Pi plugin update:** a printer that lost its pairing no longer **retries every few seconds forever** in the background; it now backs off to the steady interval, cutting wasted battery, data and Edge-function load. Get it from Moonraker's update panel. |
| **v0.9.32** | **Chamber temperature detection fix + calmer idle notification.** **(1)** The dashboard could fail to show the **chamber temperature** on some setups, in particular a **combined chamber sensor** (several probes averaged into one, `temperature_combined`) or one whose name uses **capital letters**, and especially over the **remote tunnel**, where a cold-start hiccup on the first poll could make detection give up for the whole session. Detection now retries instead of giving up, prefers the sensor named exactly `chamber` over its sub-sensors, and matches the name case-insensitively. **(2)** When **"Show only online devices"** is on and every printer is currently offline, the ongoing status notification now reads **"Watching your printers"** instead of the alarming-looking **"No printers online"**. App-only update |
| **v0.9.31** | **"Show only online devices" toggle now sticks across restarts.** The toggle added in v0.9.29 **saved** your choice but the app **never read it back at startup**, so it reset to **off** on every launch. The background notification service reads the saved setting directly and kept honouring it - so the switch could read **off** while the filter was really **on**, and if every printer was offline you were left with a persistent **"no printers online"** notification with no obvious way to clear it (toggling the switch would have turned it *on*). Startup now loads the saved value, so the toggle reflects its real state and switching it off clears the notification. App-only update |
| **v0.9.30** | **Preheat & heat-soak from the dashboard.** **Long-press a tile's hotend/bed temperatures** (while the printer is **online and idle**) to open a **Preheat** sheet: enter a **hotend and/or bed** target and tap **Set** to send `SET_HEATER_TEMPERATURE` to that machine over the existing secure path (no Pi update). **Either box can be left blank** to heat just one - the hotend for a filament change, the bed to warm up before a print. Add an optional **heat-soak timer** (minutes) and Moongate posts a **"Heat-soak complete"** notification when it elapses, so you know the machine is fully warmed through before starting; **0 = no timer**. The heater **object names are auto-detected** (`available_heaters`), so it works whether your config calls them `extruder`/`heater_bed` or something custom. The soak alert uses the **print-notification service**, so it needs **Print notifications** on - the sheet flags that (with a one-tap enable) when they're off. App-only update |
| **v0.9.29** | **"Show only online devices" notification toggle.** A new switch under **Print notifications** in the menu. When on, the ongoing **status notification lists only online printers** - offline or switched-off machines are hidden from the list, so it stays focused on what's actually running; turn it off to list every printer as before. The notification service keeps running either way (this only filters what the status list shows), and the setting travels in your backups. App-only update |
| **v0.9.28** | **Notification no longer shows a switched-off printer as "Printing".** On setups that keep the **Raspberry Pi powered** when the printer's **mainboard is switched off** (common, with a separate Pi supply), Moonraker keeps answering but Klipper is shut down and its last print state is **frozen** - so the status notification could keep showing **"Printing"**, with a stuck progress card and no live temperatures, for a printer that's actually off. The notification now reads Klipper's engine state (its **webhooks** state - the same signal the dashboard already uses for the post-emergency-stop restart button) and shows the printer as **Offline** when Klipper isn't running, clearing any stuck print card. App-only update |
| **v0.9.27** | **Print notifications stay in order.** The live **print card** for a running job now stays **pinned directly beneath the status notification** instead of the two **swapping places** in the notification shade. They sat on different importance levels - the card was "high" so it could buzz and pop up, the status list "low" and silent - and Android stacks high above low, re-sorting the two on every poll. The card now **shares the status list's quiet channel**, so it sits with it (just under it) and holds position. **Trade-off:** a print **starting or finishing no longer buzzes or pops up** - the card appears quietly under the status list and updates there. App-only update |
| **v0.9.26** | **In-app update from the pop-up + a flatter live print notification.** **(1)** The **Update** button inside the in-app **"What's new" / changelog pop-up** now runs the **in-app updater** - downloading and installing the new version with a progress bar (Android), the same as the update **banner's** Update button - instead of opening the browser to GitHub. A **"View on GitHub"** option remains for a manual download. (The banner's Update has installed in-app since v0.9.17; this brings the pop-up's button in line.) **(2)** The **live per-print notification** no longer hides its detail behind an expand. In the collapsed notification shade the **progress bar sat in place of the text line**, so the print details were only visible once you pulled the card open. The bar is **removed**, so the **progress %, time remaining, finish time and temperatures** now show **on the collapsed notification itself** - no expanding needed. App-only update |
| **v0.9.25** | **Reworked print notifications: a status-only roster + a live card per print.** The ongoing notification is now a clean **status list** - every printer with just its state (**Printing / Ready / Idle / Offline**), no progress numbers or temperatures. Each **active print** gets its **own separate notification card** with live **progress, time remaining and the projected finish time**; it appears when the print starts and updates as it runs. When a print **finishes**, its card collapses to a **"Finished &lt;time&gt;"** summary you can clear - **swipe it away or tap Clear** and the print is also reset on the printer, so the dashboard's **Done** badge and the status list settle back to **Ready** together (clearing it from the dashboard removes the card too). The separate pop-up alerts are gone - the card is now the single surface, buzzing once when a print starts and once when it finishes. Pick what each card shows under **Print notifications → Notification content**. App-only update |
| **v0.9.24** | **Tile temperatures show current only - keeps the E-STOP on screen.** Each printer tile's temperature row showed **current/target** (e.g. `265°/265°`) for the hotend, bed and chamber. On a running print the extra `/target` widened the row enough to push the **Emergency-stop** button off the edge of the tile. Tiles now show **just the current temperature**, so the row stays compact and the E-STOP stays on screen. The heater icon still takes its accent colour while a target is set (so it's clear it's heating), and the live **print notification** is unchanged - it still shows the target. App-only update |
| **v0.9.23** | **Icon guide: E-STOP entry + custom-theme colours.** The in-app icon guide (menu → About) now documents the **Emergency-stop** control (double-tap the red triangle → Klipper `M112`). On the **Custom theme**, the guide's E-STOP icon uses your configured E-STOP colour and the accent-coloured icons follow your accent, so the guide matches your themed dashboard. App-only update |
| **v0.9.22** | **Compact-tile alignment + power button.** **(1)** A webcam-hidden (compact) tile used a smaller right margin than a full tile, so its connection icons and E-STOP button sat a few pixels further right and didn't line up with a full tile stacked above or below it - the compact tile's right inset now matches the full tile. **(2)** The **power button** now appears on compact tiles too (it previously lived on the webcam, so it vanished when the webcam was hidden) - it leads the row, before the printer's name, and self-hides for printers with no power control. App-only update |
| **v0.9.21** | **Custom E-STOP colour + matching compact tiles.** **(1)** The Custom theme gains an **E-STOP** colour slot - set the colour of the emergency-stop button's ring and icon (defaults to the classic emergency red). **(2)** Compact (webcam-hidden) tiles now mirror the full tile's info band: the **E-STOP moves onto the temperature row** and the **Wi-Fi/Local + cloud/Tunnel** indicators appear - the same UI, just without the camera feed. App-only update |
| **v0.9.20** | **Full-screen custom background.** When you set a custom dashboard background image (Custom theme), it now covers the **whole screen** - the app bar and scaffold are made transparent so the image extends behind the **top bar and status bar**, instead of stopping at the dashboard body. App-only update |
| **v0.9.19** | **Cleaner emergency-stop button.** Simplified the dashboard **emergency-stop** control to a clean round button - a 2px red rim and the translucent red fill - dropping the soft glow added in v0.9.18. The post-shutdown **restart** button matches it in orange. App-only update |
| **v0.9.18** | **Readable Light-theme temperatures + a redesigned emergency-stop button.** **(1)** On the **Light** theme, the temperature readouts under each printer's name (hotend, bed and chamber) - both the text and the icons - used a near-white colour carried over from the Dark theme, so they blended into the light tile background and were hard to read. They now use the same neutral colour as the rest of the tile (matching the **"Ready"** label above the name), so they stay legible on **both** the Light and Dark themes. **(2)** The dashboard **emergency-stop** control is now a round, red-rimmed button with a soft red glow - more like a physical machine E-stop - and the post-shutdown **restart** button matches it in orange. App-only update |
| **v0.9.17** | **Mainsail-matching progress, emergency stop & in-app updates.** **(1) Accurate progress.** The dashboard progress bar/percentage and the print notification now report the **same percentage Mainsail shows** - its default **"file position (relative)"** calculation, mapping the printed byte position onto the slicer's gcode body - instead of the tile preferring an elapsed-time ÷ slicer-estimate figure that ran several percent ahead (the slicer's time estimate is routinely off). Progress is now computed in **one shared place** for both the tile and the notification, so the two can't drift apart from each other or from Mainsail. The slicer estimate is still used for the notification's **finish-time ETA**, just not for the progress bar. **(2) Emergency stop.** Each **online** printer tile gains a small **red warning triangle** beside the temperature readout - **double-tap** it to fire an immediate **emergency stop** (Klipper `M112`). It's a double-tap with **no confirmation dialog** so a single stray tap can't halt a print; once stopped, the **triangle turns into a restart button** - a single tap fires `FIRMWARE_RESTART` to bring the machine back online (deliberately manual, not automatic, so you can check why it stopped first). **(3) In-app updates.** When an update is available, the **Update** button now downloads the new APK **inside the app** with a **progress bar** and launches the installer directly, instead of opening the browser. Android still shows its standard install confirmation (a sideloaded app can't install silently), and any failure falls back to the browser. Android-only. App-only update |
| **v0.9.16** | **Offline printers cost nothing in the background.** The app now learns each printer's online/offline state from the cloud over a live **Realtime** connection (which isn't a billed API call) and **skips requesting access for printers that are switched off** - across both the dashboard and the 24/7 print-notification service - instead of pinging them every few seconds. Result: less **mobile data** and **battery**, offline tiles settle to "offline" faster, and far fewer backend calls. Online printers are unchanged, and LAN still works even if the cloud is unreachable. App-only update |
| **v0.9.15** | **Smarter camera-feed rates + printers ready from the first tap.** **(1) Network-aware feed rate.** The Local and Tunnel camera-feed rates (v0.9.14) now follow your **phone's network** rather than only the connection path: the faster **Local** rate is used whenever you're on **Wi-Fi** - including away from home, reaching a printer over the tunnel - and the throttled **Tunnel** rate applies only on **mobile data**, where it saves cellular data. (Reaching a printer on the LAN is always Wi-Fi, so home is unchanged.) **(2) Pre-warm at startup.** Every printer's full Mainsail/Fluidd page is now **warmed in the background as soon as the app starts**, so the **first** time you open a printer it's **instant** - no "Initializing…" - extending the v0.9.8 keep-warm to cover the first open too. **(3) Leaner cloud/data usage.** The app and the 24/7 background notification service now reuse a **cached access token** (~5 min) instead of re-minting one from the cloud on every status poll, and no longer re-request a token for a **powered-off** printer on every poll - cutting redundant Supabase Edge Function calls (and mobile data) substantially. App-only update |
| **v0.9.14** | **Separate Local & Tunnel camera feed rates.** The single "Dashboard camera feed" rate is now **two** rates, set on a new **Dashboard Camera Feeds** sheet (menu): a **Local feed polling rate** (Raw / 1s / 3s / 5s) used while a tile is connected over the **LAN**, and a **Tunnel feed polling rate** (Raw / 1s / 3s / 5s) used while it's connected over the **remote tunnel**. Each tile picks the right one automatically from its current connection - so you can run a fast/raw feed at home and throttle the remote feed to save mobile data. Your existing single setting **migrates into both** (no change on update); brand-new installs default to **Local 1s / Tunnel 3s**. App-only update |
| **v0.9.13** | **Drag-to-reorder is back, and restore reproduces your exact dashboard.** **(1) Drag-the-tiles reordering.** v0.9.12 (the per-printer Webcams / masonry update) temporarily swapped the dashboard's drag-the-tiles reordering for a plain single-column list. This restores the original workflow: turn off **"Auto-arrange by status"** in the menu, tap the **drag-drop button** (bottom-left, opposite the +), then **drag the tiles around the dashboard** and tap the green **check** to lock them in. Both **full** and **compact** (webcam-hidden) tiles can be dragged; while arranging they sit in a uniform grid (alignment isn't the point there) and the tidy **masonry packing returns** the moment you finish. **(2) Restore matches the backup exactly.** Restoring a backup used to only *add* printers that weren't already present - ignoring the saved order and per-tile settings of printers you already had. Now restore rebuilds the dashboard to match the backup: the saved **printer order** and each tile's config (including **hidden-webcam / compact** tiles), adding any missing printers and (after confirming) removing any that aren't in the backup. Because removing is destructive, if your phone has printers the backup doesn't include, restore **asks first and lists them** before replacing - so you can't silently lose a printer you paired after the backup. On a fresh install there's nothing to remove, so it just restores as before. Removed printers stay paired (local change), so they can be re-added or restored later. App-only update |
| **v0.9.12** | **Show or hide each printer's webcam + see-through tiles.** **(1) Webcams.** The menu's **Webcams** entry now opens a sheet listing every printer, each with an on/off switch. Turn one **off** and its dashboard tile collapses to a **compact card** - name, connection, status and temperatures, with **no camera** - and the dashboard **packs the full and compact tiles together** (a masonry grid), so the printers you're not actively watching take up far less room. A hidden webcam also **stops that tile fetching its camera**, so it's a per-printer **data saver** too - this replaces the previous all-or-nothing "Show webcams" toggle with per-printer control. **(2) Tile opacity.** On the **Custom theme**, a new **Tile opacity** (0-100) lets your dashboard background show **through** the tiles, while the camera feed stays solid. **(3) Translations.** The **Advanced Power Switch** and the new tile-opacity controls are now translated across all **8 languages**. App-only update |
| **v0.9.11** | **Clear a finished print from the tile + a custom dashboard background.** **(1) Dismiss a finished job.** When a print ends, its tile shows a **Done** (or **Cancelled**) badge - tap the small **×** on it to clear the job and send the printer back to **Idle**. One tap, no confirm (the × only appears on those finished states, so a stray tap can't disturb a live print), and the print notification follows to Idle on its next poll. Under the hood it sends Klipper's `SDCARD_RESET_FILE` through the existing proxy. **(2) Custom dashboard background.** On the **Custom theme**, the theme screen gains a **Dashboard background** option - pick any image from your phone to sit behind the tiles. It's **centred and scaled to fit over your theme colour**, so a right-sized photo fills the screen, a wrong-aspect one is centred (letterboxed), and a logo PNG sits in the middle with the theme colour showing around and through it. The **×** clears it. **(3) Refreshed icon guide.** The in-app guide (menu → About) now documents all the newer tile buttons - Print a file, Macros, Power, Lighting, Camera, Camera setup - and the new clear-a-print ×. **(4) Appearance & menu tidy-up.** The cold-start loading screen now matches the **dark** theme - dark, with the red **Moongate** brand mark, and no white flash on a light-mode phone; the **System default** theme option is removed (themes are now **Dark / Light / Custom**, default Dark); and **Settings + Language** scroll with the rest of the menu, leaving only the **version** pinned at the bottom. **(5) Advanced Power Switch.** A new menu screen (beside Lighting) for printers whose power is a **Klipper macro** rather than a Moonraker `[power]` device - set an **On + Off** pair or a single **Toggle** macro, and the tile's power button drives the printer through it. With on/off macros it asks **Turn on / Turn off** (the state can't be read, so it never assumes); the light bulb behaves the same when it has on/off macros and no toggle or status source. App-only update |
| **v0.9.10** | **Power your printer from its tile + tidier idle tiles.** **(1) Power on/off button.** If a printer has a Moonraker **power device** (a `[power …]` section - any type: GPIO, Shelly, TP-Link, Tasmota…), a **power button** now appears in the **bottom-left** of its camera tile. Tap it and **confirm** to switch the printer on or off. It works **even when the printer is off** (Moonraker stays up), so you can wake a powered-down printer right from its tile; powering off is blocked while a print is running for devices Moonraker locks. Detected automatically - no setup. **(2) Tidier idle tiles.** A printer that's offline or idle (Klipper not running) now shows **just its name** instead of a meaningless `0°/0°` temperature row. **(3) Show-webcams switch.** A new **Show webcams** toggle (menu → Camera feed) turns **all** dashboard camera feeds off or on at once - a quick data saver that also lets on-demand cameras (go2rtc) idle; the full-screen camera view still works on demand. App-only update |
| **v0.9.9** | **Finish-time ETA in notifications + a notification you can customise.** **(1) When will it finish?** The live print notification now shows the **projected finish time** as a clock time (e.g. **1:20 PM**), right next to the time remaining - the same **ETA** Klipper and Mainsail display. It follows your app's language for 12- or 24-hour format. **(2) Make the notification yours.** A new **Notification content** screen (under **Print notifications** in the menu) lets you choose **which details appear** - progress, time remaining, finish time, hotend temperature, bed temperature - **drag them into any order**, and switch off the ones you don't want (e.g. show the finish time but hide the running time). A **live preview** updates as you tweak, and your choice is **saved and travels in your backups**. **(3) Tidier menu.** The **Lighting** entry now sits right under **Font size** for quicker reach. App-only update |
| **v0.9.8** | **Instant printer re-open + lighting control.** **(1) Re-opening a printer is now instant.** A printer's full Mainsail/Fluidd interface used to reload from scratch every time you opened its tile - replaying Mainsail's **"Initializing…"** while the page and its live connection rebuilt, which is slow over the tunnel (away from home). Now each printer's page is **kept warm in the background**: the first open loads it as before, but afterwards, going back to the dashboard and re-opening is **instant**, with the connection still live. Pages are kept for every printer you've opened; if the phone runs low on memory, the least-recently-used ones are released automatically. **(2) Control your printer lights from the dashboard.** Set up which printers have a controllable light under the new **Lighting** menu - for each, choose an **On + Off** macro pair *or* a single **Toggle** macro (whichever your setup uses), and optionally point Moongate at the light's Klipper object (e.g. `output_pin caselight`). A **bulb** then appears in the corner of that tile's camera: tap it to switch the light on/off, and it glows when the light is on - reflecting the real state even if you toggle it elsewhere. App-only update |
| **v0.9.7** | **Squarer, tidier dashboard tiles.** Each printer tile's live **camera feed is now a clean 1:1 square** - it was cropped to an odd wide-and-short shape before - with the name and temperatures tucked underneath. **Offline printers** no longer leave a blank gap: an unreachable tile **fills with its Mainsail/Fluidd logo** and shows just the **name** (there are no live temperatures to report while offline), so it sits flush instead of stretching. And while a printer is **printing**, the **macro button hides** so the progress bar and percentage keep their width and stay easy to read - it returns once the tile is idle or finished. App-only update |
| **v0.9.6** | **Run your Klipper macros from the dashboard.** Every online printer tile gains a **macro button** (the `<>` icon, next to the folder) that opens the list of macros defined on that machine - your `[gcode_macro …]` sections, read live from the printer. **Tap one to run it**, after a quick **confirm** (some macros heat the bed, move the toolhead, or cancel a print, so a stray tap shouldn't fire them). **Star the ones you use most** and they pin to the **top** of the list - useful when a machine has dozens of macros but you reach for only a handful; your stars are saved per-printer and travel in your backups. Internal **helper macros** (names starting with `_`) and Moongate's own **pairing macros** (`MOONGATE_PAIR` / `MOONGATE_RESET_OWNER`) are hidden to keep the list to the macros you'd actually run. Also tidied the tile controls - the firmware-restart button now shows only when a printer has errored, not on every idle tile. App-only update |
| **v0.9.5** | **Tap a camera to watch it full-screen.** Every printer tile that's showing a camera now has a small **eye button** in the corner - tap it for a **centred, full-screen live view** you can **pinch to zoom** and pan. **Turn your phone sideways** and the view rotates to **widescreen**, even though the rest of the app stays in portrait - it returns to portrait when you close it. The same full-screen view also opens from the **camera icon** on the printer page. App-only update |
| **v0.9.4** | **Arrange your printers your way.** Turn off **"Auto-arrange by status"** in the menu and the dashboard stops re-sorting tiles by activity - instead you can **drag the tiles into any order you like**. Tap the **drag-drop button** (bottom-left, opposite the +) to start arranging, drag the tiles around, then tap the green **check** to lock them in. Once locked, tiles stay put and a tap opens the printer as usual. Your custom order and the auto-arrange on/off choice are both saved and travel in your backups. App-only update |
| **v0.9.3** | **Clearer notification category names.** The two Android notification categories read backwards: the **always-on status list** of your printers sat under *"Print progress"*, while *"Print status"* actually controlled the **pop-up alerts** (started / finished / cancelled). They're now named for what they do - **"Print status"** (the live list) and **"Print alerts"** (the pop-ups). No behaviour change, and your existing on/off choices carry over. App-only update |
| **v0.9.2** | **The webcam works on the printer page when you're away from home.** Opening a printer embeds the real Mainsail/Fluidd page, whose webcam panel uses the camera's own address - so an **external camera** (e.g. an old phone running an IP-webcam app at a `192.168.x.x` LAN address) **can't load over mobile data**, only at home. This adds a **native camera view**: open a printer and tap the new **camera icon** in the top bar for a full-screen live feed that routes through your printer's secure tunnel - the same path the dashboard tile preview already uses - so the external camera streams remotely too. A one-time **hint** appears under the title when a printer's camera is the kind that can't load in the embedded page remotely, pointing you to the camera view (it never shows for a normal Pi webcam, which already works remotely). App-only update |
| **v0.9.1** | **See what's new before you update.** When an app update is available, the banner now has a **"What's new"** button beside Update - tap it to see everything that's changed from your installed version up to the latest, right in the app. (Previously the in-app "What's new" only listed changes up to your installed version, so you couldn't preview a pending update.) App-only update |
| **v0.9.0** | **External network cameras.** You can now point a dashboard tile at a camera that isn't wired into Klipper - like an old phone running an IP-webcam app. Tap the small **gear** in the corner of a tile's camera and enter the camera's address (the same URL you'd use in Mainsail's webcam settings, e.g. `http://192.168.0.107:8080/video`). Cameras you've **already set up in Mainsail are detected automatically** - they just appear, and the gear lets you override or correct them. Works on **Wi-Fi**, and **remotely** through your printer's secure tunnel (for cameras on your home network). A **"Camera config icons"** toggle in the menu hides the gears if you don't use them. **Re-run the Pi installer (or update via Mainsail → Software Updates)** for the auto-detect and remote-camera support; setting a camera by hand and viewing it on Wi-Fi works without a Pi update |
| **v0.8.5** | **Clearer reconnect after an identity wipe.** If the app loses its cloud session - most often the anonymous sign-in being **rate-limited** after several quick reinstalls - the dashboard now shows a **"reconnecting… retrying"** banner and keeps retrying in the background, instead of silently showing every printer offline with no explanation. Your printers come back on their own once sign-in succeeds - no need to force-stop or restore. App-only update |
| **v0.8.4** | **Faster re-pairing, an honest pairing-time hint, and clearer scrolling.** **(1) Re-pairing in seconds.** Re-pairing a printer (reinstall, new phone, reset-owner) now reconnects in seconds instead of minutes - the Pi reports its tunnel URL to the freshly-created cloud record straight away instead of waiting for the next 5-minute heartbeat. **(2) Honest pairing-time hint.** The GATE-code notice now says the printer can take up to about a minute (it's waiting for the secure tunnel) - replacing the stale "up to ~10 minutes". Scanning the QR is still an instant local connection. **(3) Scroll affordance.** The hamburger menu and the Add Printer screen now show a scrollbar, so it's obvious there's more below the fold on smaller screens. Re-run the Pi installer (or update via Mainsail → Software Updates) for the faster re-pairing |
| **v0.8.3** | **Add-printer keyboard fix + a cleaner brand mark.** Two small things. **(1) A keyboard you can always get back.** On some Android keyboards, tapping a text field *again* after dropping the keyboard didn't bring it back - so if you mistyped a printer's name while pairing, you could get stuck unable to fix it. Tapping the field now reliably re-opens the keyboard, and the name and address fields gain a small **keyboard button** to summon it on demand (on the **Add Printer** screen and the printer rename dialog). **(2) Transparent brand mark.** The moon-gate logo in the dashboard header now sits on a transparent background instead of a dark square, so it looks clean against any theme. App-only update |
| **v0.8.2** | **Print straight from your printer's files, a real app icon, and notification fixes.** **(1) Print a stored file.** A new **folder button** on a ready printer's dashboard tile opens the **G-code files already on it** - shown with their slicer **thumbnails**, newest first - so you can pick one and **Start print** (with a confirm tap). **(2) A proper app icon.** Moongate finally has its own icon - a red "moon gate" ring + crescent on black - across the launcher, the dashboard bar and notifications, replacing the stock Flutter logo. **(3) Notification fixes.** A powered-off printer now correctly shows **Offline** instead of "Idle", and switching between Wi-Fi and cellular no longer re-fires a spurious **"print cancelled"** alert. App-only update |
| **v0.8.1** | **Print-notification refinements.** Notifications refresh the moment you add, remove or restore a printer (no more "No printers" stuck after a restore); a new **Update frequency** setting (5s / 10s / 15s / 30s / 1 min, under Print notifications) controls how often they poll; status updates promptly when a print errors or finishes; and a printer recovering from an error pops a "ready" alert. App-only update |
| **v0.8.0** | **Optional print notifications.** Turn them on (**Menu → Print notifications**) for a live status of every printer in your notification shade - per-printer progress, ETA, temperatures and heat-up, plus alerts when a print starts, finishes, pauses or errors. Works with the app in the background; off by default. Also: backups now carry your **settings** (theme, custom colours, dashboard columns, language, camera refresh) alongside the printer list; and printers **sort by activity** - whatever's printing floats to the top, then Ready, Idle, Offline - on both the dashboard and the notification. App-only update |
| **v0.7.0** | **Moongate now speaks your language.** The app is fully translated into **8 languages** - English (default) plus German, French, Spanish, Italian, Simplified Chinese, Russian, and Polish. You pick your language on first launch (before pairing), and can change it anytime from **Language** at the bottom of the menu. Also: a new **icon guide** explains every dashboard icon at a glance, with a clear "Back to dashboard" button; and single-column dashboard tiles are now square for a tidier layout. Translations are machine-assisted best-effort - corrections are a one-line ARB change and community PRs are very welcome. App-only update |
| **v0.6.5** | **A quick "How pairing works" guide so new users don't get stuck.** Moongate now shows a short explainer on launch (and anytime from **Menu → How pairing works**) covering the three things people miss: you **pair a printer once**; **app updates keep your printers** (no re-pairing); and after a **reinstall or new phone** you either **Restore** from a backup (brings them back online, no re-pairing) or run **`MOONGATE_RESET_OWNER`** on the printer and pair again. Tick **"Don't show this again"** to dismiss it. App-only update |
| **v0.6.4** | **Critical fix: remote access over the internet works again.** A v0.6.3 change accidentally broke the Pi's tunnel auth proxy, so any request to a printer **over the internet (away from home Wi-Fi) returned a 500 server error** - local Wi-Fi access was unaffected, which is why it slipped through. This restores remote access for everyone. **Re-run the Pi installer to deploy the fix.** Also: **(2)** Restore is now honest about its results - it distinguishes printers that genuinely came back online from those that still need a re-pair (the old message claimed "reconnected" even when nothing was re-linked). **(3)** Bug reports now capture the **remote (tunnel) connection result** and the **Pi's plugin version**, closing the two biggest blind spots when triaging connection problems. Re-run the Pi installer for the tunnel fix |
| **v0.6.3** | **Restore actually brings your printers back - plus a built-in bug reporter.** Three things. **(1) Backup restore that reconnects.** Your config backup now carries a single-use restore code, so after a reinstall - or on a new phone - **Restore config** brings your printers back **online automatically, with no re-pairing.** (The old backup saved only your printer list, so every printer still had to be re-paired; this one re-links them to the freshly-installed app.) **Re-run the Pi installer** to pick up the matching Pi-side change that lets the printer recognise the restored app - without it, restored tiles stay on "Connected / idle". **(2) Import from the first screen.** The **Add Printer** screen gains an **Import config from file** option, so a reinstalling user can restore *before* (or instead of) pairing - previously restore lived only in the menu, unreachable until you'd added a printer. **(3) Report a problem.** A new **Menu → Report a problem** sends a bug report to the developer with diagnostics attached automatically - app version, device, your network, and each printer's connection state - so connection / pairing issues can be fixed without a back-and-forth. There's also a **"Trouble pairing? Send a report"** link on the Add Printer screen for when a printer won't pair. Re-run the Pi installer for the restore-reconnect change |
| **v0.6.2** | **Easier pairing on unusual networks + a steadier first connection.** Three things. **(1) Set a printer's address by hand.** Add Printer gains an **Advanced - custom network** option, and each printer's edit dialog now lets you set its **address** (e.g. `192.168.1.50:7125`). This is for printers the app can't discover automatically - typically ones behind a reverse proxy (Traefik, Caddy, Nginx Proxy Manager) or running in Docker. Enter the same address you'd use to open the printer's web page (Mainsail / Fluidd) in a browser. **(2) Clearer pairing choices.** Scanning the QR is now marked as the instant method; typing the GATE code is flagged as the slower alternative, since without the QR the app has to find the printer on your network - which can take a few minutes. **(3) Steadier first connection.** A freshly-paired printer is less likely to flash a premature "Offline", and a tile that went stale while the app was in the background refreshes the moment you return to it. App-only update |
| **v0.6.1** | **Fixes a printer stuck on "Starting up…".** A printer paired while powered off - or that went offline right after pairing - could sit on **"Starting up…"** forever, because it never reported a remote connection for the app to give up on. The app now recognises when a printer is genuinely unreachable on every path and shows **Offline** instead, while still allowing a brief grace window for one that's legitimately just booting. App-only update |
| **v0.6.0** | **App lock + a lighter, cleaner app.** Two things. **(1) App lock.** A new optional **App lock** protects Moongate with a PIN - and, if your phone has it, your fingerprint or face. The lock appears whenever the app is opened fresh; you can optionally have it re-lock after the app has been in the background for a while (Immediately / 1 / 5 / 15 minutes, or only on launch). Your PIN is stored encrypted on the device and repeated wrong guesses are rate-limited, with a "Forgot PIN?" reset as a fallback. It's off by default - turn it on under **Menu → App lock**. **(2) Slimmer permissions.** Removed a long-dead WireGuard/VPN code path left over from early development. Nothing in Moongate used it, and dropping it means the app no longer requests the VPN or special foreground-service permissions it used to. App-only update |
| **v0.5.2** | **Back up to a file + local Wi-Fi progress fix.** Two fixes. **(1) File backup & restore.** **Back up config** and **Restore config** in the menu now save to and load from a file you pick (Android's document picker), replacing the old copy-to-clipboard flow that didn't survive reinstalling the app reliably. Your printer list is written to a `moongate-printers.json` file you choose, and restored the same way. (As before, the backup holds your printer list only - not the app's identity - so after a reinstall you'll still re-pair each printer.) **(2) Local Wi-Fi progress & chamber temp.** Fixed print progress and chamber temperature showing blank/zero when viewing a printer over local Wi-Fi - a supplementary status request was being rejected on the LAN path. Remote (tunnel) access was unaffected. App-only update |
| **v0.5.1** | **Instant "Local" the moment you pair + dashboard data-saver.** Two things in one release. **(1) Pairing is instant.** Scan the QR (or type the GATE code) and the printer now appears on the dashboard as **Local right away**, with remote (tunnel) access syncing quietly in the background - no more sitting on "Starting up… waiting for first heartbeat" for minutes. The pairing QR now carries the Pi's local address, so the app connects directly over your home WiFi the instant you pair, then lights up the tunnel icon once remote is ready too. (The address is a local-network-only hand-off - it never travels through the cloud.) **(2) Camera data-saver.** The dashboard's live webcam thumbnails were refreshing as fast as your camera allowed - a constant data drain, especially over the tunnel. There's now a **Dashboard camera feed** setting in the menu (Raw / 1s / 3s / 5s, default **1 second**) that caps how often tiles refresh, and tiles stop fetching entirely while the app is in the background. Re-run the Pi installer to pick up the instant-pair change |
| **v0.5.0** | **LAN discovery via mDNS - survives IP changes and internet outages.** Companion to the v0.4.4 Pi-side mDNS advertisement. The app now browses for `_moongate._tcp.local` services on the home WiFi and uses each discovered URL as the authoritative LAN address for the matching printer. Effect: when your Pi's IP changes (DHCP renewal, router reboot, ISP-supplied gateway swap), the dashboard tile flips back to "Local" on the next poll cycle instead of waiting for a tunnel-mediated recovery - even if the internet leg is down. Browses fire on app foreground, every ~minute while the dashboard is open, and once at cold launch in parallel with Supabase init. Falls through to v0.4.x behaviour when nothing is discovered (router blocks multicast, Pi isn't running v0.4.4+, phone on a different subnet). No new permissions; uses Android's `NsdManager`. While we were there: `pause/resume/cancel` now reads the live LAN URL from the registry on every action instead of capturing it at the tile's construction time, so an IP change picked up by the status service propagates to controls without waiting for the tile to be rebuilt |
| **v0.4.4** | **Pi-side groundwork for LAN discovery (v0.5.0 preparation).** After a successful pair, the Pi now advertises itself on the local network via mDNS (Avahi) as `_moongate._tcp.local` with its `printer_id` and HTTP port. No user-visible behaviour change in this release on its own - the v0.5.0 app will browse for these advertisements and use them as the authoritative LAN address, so the dashboard tile stays "Local" through DHCP IP changes / router reboots even while the internet leg is down. To pick up the change, re-run the Pi installer (`curl -fsSL …/install.sh \| bash`) - it adds the required tightly-scoped sudoers entry and ensures `avahi-daemon` is enabled. App side unchanged; same v0.4.3 behaviour against a v0.4.3 Pi. See `docs/v0.5-lan-discovery-design.md` for the full design |
| **v0.4.3** | **Webcam tile fix for slow camera servers.** Dashboard webcam thumbnails were stuck on the Mainsail / Fluidd logo placeholder for any printer whose webcam server took longer than ~33 ms to return a snapshot - most commonly **stock RatRig Micron+ machines running uv4l-mjpeg**, where each snapshot can take 3 s. The tile's old fetch loop ticked at the configured 15-30 FPS and cancelled every in-flight `Image.network` load with the next tick, so no frame ever completed. The fetch loop is now strictly sequential: the next snapshot only kicks off after the previous one finishes (success, error, or timeout). Fast Crowsnest setups still hit the configured rate; slow ones self-throttle to whatever they can sustain instead of looking dead. Tunnel-mode 3 FPS hard cap removed in the same change - the new loop is naturally bounded by round-trip latency. Pi-side install is unchanged; just update the app |
| **v0.4.2** | **Pairing quality-of-life + cellular reliability.** Re-pairing a printer after a fresh app install no longer bounces off "already paired" - `MOONGATE_RESET_OWNER` now releases the cloud row too (Pi proves it with its device key). The new printer appears with live data within seconds of pairing instead of waiting for the next ~5-minute heartbeat. Mainsail through the tunnel stays connected on cellular instead of dropping every 30-60 seconds. And the Add Printer screen has a typeable "GATE code" field as a fallback for when the camera scan can't read the QR - type the code shown on the Klipper console |
| **v0.4.1** | **Dashboard webcam preview fix.** The dashboard tile now shows live snapshots from your printer's webcam on both local network and tunnel. They were stuck showing the Mainsail / Fluidd logo placeholder on v0.4.0 - a regression dating back to v0.3 that became widely visible after v0.4's public release. Pi-side change not required; the fix is entirely in the app's URL construction. Just update the app |
| **v0.4.0** | **Hardened remote access.** Every internet-facing path on the Pi now sits behind a small auth proxy that requires a short-lived signed token on every request. Sharing the tunnel URL gives an attacker nothing - the URL returns flat 401s with no fingerprint hinting at what's behind it. This closes the "visit the tunnel URL in a browser and you see Mainsail" hole noted in v0.2.29's caveat. The full Mainsail / Fluidd UI is reachable through the tunnel from anywhere, including cellular. Dashboard tile gains a "Connected - Printer idle" state for when the Pi is up but the printer side isn't (e.g. K3 with its printer-power toggle off), and the detected web UI (Mainsail or Fluidd) is now persisted per printer so the logo shows immediately on a cold launch even before the first poll succeeds. LAN is retried on every poll - walking back into home WiFi after a brief tunnel hop now flips the indicator back to "Local" on the next poll cycle. Side-menu additions: a brief "What's new" dialog (this page in app form) and a "Buy me a coffee" link |
| **v0.3.0** | **Cloud-mediated pairing.** Replaces the v0.2 shared-URL-plus-local-token model with a small cloud middleman that handles anonymous sign-in (no email, no password - the app creates an anonymous identity on first launch), tracks the current tunnel URL per Pi (Cloudflare Quick Tunnel URLs rotate every Pi reboot), and issues a fresh short-lived access token to the app on demand. End-user impact: nothing to share, no URL to remember, no tunnel-URL re-pairing when the Pi reboots. The Remove printer flow now releases the cloud association so re-pairing the same Pi just works. Hard cutover from v0.2.x - fresh install required; v0.2.x local state moved aside to `~/.config/moongate/legacy-v0.2.x/` |
| **v0.2.29** | Incident-response improvements after the "shared the tunnel URL with a friend and they got control" scenario: (1) QR pre-issued tokens are now **provisional** - they carry a 10-minute initial TTL even though the JWT itself is signed for 30 days, and only "promote" to the full 30 days on first actual use. A shared but unscanned URL self-expires inside 10 minutes instead of 30 days. (2) New `MOONGATE_REVOKE_ALL` macro for one-shot token nuking after an incident. (3) New `MOONGATE_LIST_TOKENS` macro that prints every issued token (active/expired/revoked) to the Mainsail console for auditing. (4) `MOONGATE_PAIR` console output now carries a prominent "⚠ DO NOT SHARE THIS URL" warning block. (5) New `TROUBLESHOOTING.md` entry walking through the full recovery sequence. **Does NOT close the underlying "browser visits the tunnel URL → gets Mainsail with no auth" hole** - that needs the v0.2.30 Moonraker `[authorization]` work next |
| **v0.2.28** | Licence switched from **MIT** to **PolyForm Noncommercial 1.0.0**. Personal, hobby, educational, and non-commercial-organisation use stays free and unrestricted; commercial use now requires a separate written licence. Releases up to and including v0.2.27 remain MIT for anyone who already has them. No code changes |
| **v0.2.27** | More reliable in-app update banner: (1) cache-buster on the `latest_version.json` fetch so GitHub's raw CDN can't serve a stale "no update" body, (2) re-run the update check every time the app comes back to the foreground so a user who had the app open before CI published a new release sees the banner appear without needing to force-close. Previously the FutureProvider's autoDispose cache could mask a freshly-published release for the entire session |
| **v0.2.26** | Auto-recover from "stuck on tunnel": once a printer's status service flipped to tunnel-first (e.g. one transient local-poll failure at startup), it would stay there for the rest of the session even after the user returned home and was sitting on the printer's LAN. The dashboard would keep saying "Tunnel" until the app was killed and reopened. Now every 20 seconds, if the phone's subnet matches the printer's, the service retries local-first for one cycle. If local succeeds it switches back; if not, the backoff prevents repeated 3 s timeouts |
| **v0.2.25** | Dashboard webcam refresh rate now mirrors the **Target FPS** you set in Crowsnest / Mainsail's webcam config. The plugin reads `target_fps` from `/server/webcams/list` and the tile derives its snapshot poll interval as `1000 / fps` ms - set Crowsnest to 15 fps, the tile ticks at 15 fps; set 30 fps, the tile ticks at 30 fps. Clamped server- and client-side to [1, 60]. Defaults to 15 fps when not configured (matches stock Crowsnest / mjpg-streamer). Persisted in `PrinterConfig` so the very first frame after cold-launch already uses the right cadence |
| **v0.2.24** | Dashboard webcam tiles bumped to 20 fps (now superseded by v0.2.25's server-driven rate) |
| **v0.2.23** | Custom-theme colour picker: the **Done** button now respects the system navigation / gesture bar at the bottom of the modal sheet. Previously the button could overlap the phone's 3-button nav row on devices using on-screen buttons |
| **v0.2.22** | Configurable HTTP port: `install.sh --port N` (also `MOONGATE_PORT` env var for piped installs); plugin reads the port from `~/.config/moongate/config.json` and embeds it in the QR + pair-page URLs; app's pair screen gains an optional **Port** field next to the IP for users with non-standard nginx setups. Plus: clarify in [SECURITY.md](SECURITY.md) and the README that the tunnel exposes Mainsail / Moonraker themselves, not just Moongate's JWT-protected endpoints - and how to mitigate (Cloudflare Access, tightened `trusted_clients`, or staying LAN-only) |
| **v0.2.21** | Custom theme: new fourth radio option in the drawer (System / Dark / Light / **Custom**) opens a full-screen colour editor. Five slots - Accent, Page background, Cards & tiles, Text, Error - each tappable to a modal sheet with HEX input + 24-colour preset palette. Live preview tile inside the editor, instant theme application across the app, reset-to-defaults action |
| **v0.2.20** | Camera scanner fix #2: add ProGuard rules for ML Kit + mobile_scanner so R8 doesn't strip the bundled barcode scanner classes (the mobile_scanner consumer rule had a single-dot wildcard bug that only matched the root package) |
| **v0.2.19** | Upgrade `mobile_scanner` 5.2.3 → 7.x - fixes the Samsung One UI `analysis.resolutionInfo!!` NPE. CameraX bumped to 1.5.3 |
| **v0.2.18** | Network-aware connection: subnet check at cold launch & on app resume pre-decides "use tunnel" for any printer whose LAN isn't reachable from the current WiFi. Symmetric `onHttpError` handling in the printer WebView so a 4xx from an unrelated device on a stranger LAN doesn't trigger the error overlay |
| **v0.2.17** | Printer screen now reads the dashboard tile's live network-path decision so tapping a tile on a foreign network jumps straight to the tunnel - no more "Printer unreachable" overlay |
| **v0.2.16** | Print progress now uses `print_duration / estimated_time` from `/server/files/metadata`, matching Mainsail/Fluidd exactly. Falls back to `display_status.progress` then `virtual_sdcard.progress` when slicer estimate is unavailable |
| **v0.2.15** | Supplementary progress query when the plugin endpoint omits `display_status` or `virtual_sdcard` |
| **v0.2.14** | QR scanner refactor: explicit `StreamSubscription` to `controller.barcodes` instead of `onDetect`, avoiding the `WidgetsBindingObserver` leak in `mobile_scanner` 5.x |
| **v0.2.13** | Force CameraX 1.4.0 to dodge a Samsung-specific NPE in `mobile_scanner` 5.x's CameraX 1.3.3 (later superseded by v0.2.19's package bump) |
| **v0.2.12** | Supplementary progress query, chamber sensor support refinements |
| **v0.2.11** | Chamber temperature on tile, supplementary chamber query for older plugin builds |
| **v0.2.10** | Silent offline retries + Mainsail/Fluidd webcam placeholder logo + grid column picker + Loading Local / Loading Tunnel / Offline labels in webcam area |
| **v0.2.9** | Always start local-first on launch; expand chamber discovery to include `temperature_fan` and `heater_generic` |
| **v0.2.8** | Dynamic chamber sensor discovery for any capitalisation / suffix |
| **v0.2.7** | Camera error diagnostic: show actual native error code/message so device-specific failures are readable |
| **v0.2.6** | Camera: explicit controller with autoStart=false + post-frame start; 700 ms delay on first permission grant |
| **v0.2.5** | Fix 5 bugs: app name capitalisation, import config crash, print controls now respect remote-first preference, router crash guard on missing printer, VPN disconnect safe on sign-out |
| **v0.2.4** | Fix camera `genericError` - switch `MainActivity` to `FlutterFragmentActivity` (required by CameraX / MobileScanner v5) |
| **v0.2.3** | Remove explicit `MobileScannerController` - let MobileScanner manage its own CameraX lifecycle |
| **v0.2.2** | Consistent release signing; fix update conflict on install; longer tunnel timeout (8 s); `startup` badge state |
| **v0.2.1** | In-app update banner; version bump process established |
| **v0.2.0** | Cloudflare Quick Tunnel remote access; auto local/remote fallback |
